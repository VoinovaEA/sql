version: "3.9"

services:

  web:
    build: .
    restart: always
    ports:
      - "8000:5000"
    depends_on:
      - redis
    volumes:
      - ./logs:/code/logs

  redis:
    image: redis:alpine
    restart: always
    ports:
      - "6379:6379"